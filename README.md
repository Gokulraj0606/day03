# day03

for a given JSON iterate over all for loops   
              var person = {
   "name": "Nick",
   "name1": "Jonas",
   age: 26
}; 
for (let x in person) {
   console.log(x + ": "+ person[x])
}
